# Decision: Cross-Dialect Parser-Error Feasibility Census

Date: 2026-05-27
Status: Accepted
Related milestone/version: v0.210.0
Related tests:
  `internal/application/audit/ddl_parser_error_feasibility_census_test.go`

## Context

v0.200.0 cleared all tracked parseable MySQL/TiDB DDL gaps by promoting 25+27 `normalized_silent` forms to `finding_covered`. The remaining high-level gap class across all three dialects is `parser_error` — forms that the parser library cannot parse at all.

v0.200.0 census baseline:

| Dialect | Total | Finding Covered | Normalized Silent | Unsupported Boundary | Parser Error |
|---|---:|---:|---:|---:|---:|
| MySQL | 61 | 46 | 0 | 0 | 15 |
| TiDB | 54 | 45 | 0 | 0 | 9 |
| PostgreSQL | 285 | 274 | 6 | 0 | 5 |

## Remaining Parser Errors

### MySQL (15)

ALTER VIEW, ALTER PROCEDURE, CREATE FUNCTION, ALTER FUNCTION, DROP FUNCTION, CREATE TRIGGER, DROP TRIGGER, CREATE EVENT, ALTER EVENT, DROP EVENT, CREATE TABLESPACE, ALTER TABLESPACE, DROP TABLESPACE, CREATE RESOURCE GROUP, ALTER RESOURCE GROUP

### TiDB (9)

ALTER VIEW, CREATE TRIGGER, DROP TRIGGER, CREATE FUNCTION, DROP FUNCTION, CREATE EVENT, DROP EVENT, ALTER TABLE TTL, ALTER TABLE LOCALITY

### PostgreSQL (5)

DROP SUBSCRIPTION ... WITH (drop_slot = true) and 4 PostgreSQL 18 ALTER TABLE grammar forms pending `pg_query_go v7`.

## Decision

v0.210.0 will classify every remaining tracked DDL `parser_error` form into exactly one feasibility bucket. No parser behavior changes, no new rules, no fallback extraction.

### Feasibility Buckets

| Bucket | Meaning |
|---|---|
| `parser_upgrade_candidate` | Likely supported by newer parser/library versions; do not fallback yet |
| `bounded_fallback_candidate` | Could be safely detected with a small bounded pre-parser fallback; no payload leak |
| `product_unsupported_or_inapplicable` | Dialect product does not support the feature or support is intentionally out of scope |
| `unsafe_fallback_defer` | Fallback would require parsing bodies, predicates, option payloads, or sensitive strings |
| `needs_research` | Evidence insufficient; must not be promoted |

### Evidence Model

For each parser-error case:

| Field | Meaning |
|---|---|
| `dialect` | MySQL, TiDB, PostgreSQL |
| `family` | trigger, event, function, tablespace, resource group, table option, PG18 ALTER TABLE, etc. |
| `name` | stable case name from census |
| `sql` | test input, only in test fixtures/logs |
| `expected` | one of the feasibility buckets |
| `reason` | rationale for the bucket assignment |

## Non-Goals

- Do not add audit rules.
- Do not change parser behavior.
- Do not add fallback extraction in this milestone.
- Do not update release surfaces until the release-surface phase.
- Do not claim parser-error forms are supported.
- Do not claim full DDL support or dialect parity.
- Do not expose raw SQL, routine bodies, trigger bodies, event bodies, passwords, catalog state, runtime validation, lock duration, or rewrite duration.

## Verification Plan

1. Baseline `ddl-census-report` confirms unchanged counts: MySQL 61/46/0/0/15/0, TiDB 54/45/0/0/9/0, PostgreSQL 285/274/6/0/5/0.
2. Feasibility census test skeleton compiles and passes.
3. `make decision-record-gate` passes.
4. No production code modified.
5. Later: `make ddl-parser-error-feasibility-report` prints bucket counts for all dialects.

## Links

- v0.200.0 decision: `docs/decisions/2026-05-26-v0.200.0-mysql-tidb-ddl-normalized-silent-coverage-pack.md`
- Census test: `internal/application/audit/cross_dialect_ddl_coverage_census_test.go`
- PostgreSQL consolidated census: `internal/application/audit/postgresql_ddl_consolidated_census_postgresql_tag_test.go`
- PostgreSQL ALTER TABLE residual census: `internal/application/audit/postgresql_alter_table_residual_census_postgresql_tag_test.go`

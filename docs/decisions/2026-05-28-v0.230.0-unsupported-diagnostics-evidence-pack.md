# Decision: Unsupported Diagnostics Evidence Pack

Date: 2026-05-28
Status: Accepted
Related milestone/version: v0.230.0
Related commits: 0bdf52b (v0.220.0 base)
Related tests: `internal/application/audit/unsupported_diagnostics_evidence_test.go`, `internal/application/audit/unsupported_diagnostics_evidence_postgresql_tag_test.go`, `pkg/deltascope/audit_unsupported_diagnostics_evidence_test.go`, `internal/interfaces/cli/cli_unsupported_diagnostics_evidence_test.go`, `internal/interfaces/http/handler_unsupported_diagnostics_evidence_test.go`, `internal/interfaces/mcp/server_unsupported_diagnostics_evidence_test.go`
Related docs: `docs/plans/2026-05-28-v0.230.0-unsupported-diagnostics-evidence-pack/design.md`

## Context

v0.220.0 standardized the parser-error diagnostic message:

> statement was not audited because the selected dialect parser could not parse it; no audit findings were inferred

This fixed raw parser payload leakage and made parser-error SQL clearly unaudited. The remaining UX gap is that the diagnostic is sparse: it says what happened, but not enough about what the user should do next or how to distinguish parser errors from structured unsupported boundaries.

Current census (unchanged since v0.220.0):

| Dialect | Total | Finding Covered | Normalized Silent | Unsupported Boundary | Parser Error | Unclassified |
|---|---:|---:|---:|---:|---:|---:|
| MySQL | 61 | 46 | 0 | 0 | 15 | 0 |
| TiDB | 54 | 45 | 0 | 0 | 9 | 0 |
| PostgreSQL | 285 | 274 | 6 | 0 | 5 | 0 |

## Decision

Add a stable structured diagnostic evidence layer for parser-error and structured unsupported outcomes. Public surfaces will expose a `Diagnostic` struct with classification, reason, action hint, audited flag, and dialect — without inferring object names from unparsed SQL.

## Rationale

- Users need actionable next steps, not just a description of the failure mode.
- Distinguishing `parser_error` from `unsupported_statement` helps users understand whether the issue is a parser limitation or a deliberate unsupported boundary.
- Structured evidence enables programmatic handling (CI gates, dashboards) without parsing prose.
- Generic action hints are safe: they avoid guessing specific object names from failed parse text.

## Public Contract

### Diagnostic Evidence Fields

| Field | Type | Meaning |
|---|---|---|
| `classification` | string | Stable category: `parser_error` or `unsupported_statement` |
| `reason` | string | Safe human-readable explanation |
| `action_hint` | string | Generic next step for the user |
| `audited` | bool | `false` for parser-error and unsupported statement-level outcomes |
| `dialect` | string | Selected dialect when available |

### Parser-Error Diagnostic

- `classification` = `parser_error`
- `reason` = v0.220.0 standard diagnostic (unchanged)
- `action_hint` = `verify the selected dialect and syntax, split multi-statement input if needed, or upgrade DeltaScope when parser support becomes available`
- `audited` = `false`
- `dialect` = selected dialect

### Structured Unsupported Diagnostic

- `classification` = `unsupported_statement`
- `reason` = safe explanation (reuses existing `UnsupportedDetail.Reason` when available)
- `action_hint` = `treat this statement as not covered by DeltaScope policy checks; review it manually or track support in a future DeltaScope release`
- `audited` = `false`
- `dialect` = selected dialect

### No-Leak Contract

Diagnostics must not contain:

- routine body, function body, trigger body, event body
- event schedule expressions
- TTL option expressions
- locality region strings
- tablespace file paths
- resource group option values
- PostgreSQL option payloads
- raw parser `near ...` fragments
- raw SQL text

The action hint must stay generic. It must not suggest a specific object name inferred from failed parse text.

## Deferred / Out Of Scope

- No parser support added.
- No fallback parser added.
- No parser dependency upgrade.
- No new SQL audit rules added.
- No parser_error count reduction.
- No conversion of parser errors into structured unsupported details by guessing.
- No full DDL support or dialect parity claim.
- No change to policy evaluation for supported statements.

### Task 4: Docs, Gates, and Milestone Readiness

- New Makefile target: `unsupported-diagnostics-evidence-test` runs evidence tests across application, SDK, CLI, HTTP, and MCP.
- Updated docs:
  - `docs/dev/testing.md`: documented the new Makefile target.
  - `docs/reference/cli.md`: added "Unsupported Diagnostics Evidence (v0.230.0)" section documenting the five diagnostic fields, parser-error and unsupported_statement classifications, no-leak contract, and explicit non-goals. Updated Repository Confidence Targets table.
  - `docs/reference/cli.zh-CN.md`: synced Chinese documentation.
  - `docs/reference/audit-capability-matrix.md`: added "Unsupported diagnostics evidence (v0.230.0)" row to Trust & Misconfiguration Guardrails table.
  - `docs/reference/audit-capability-matrix.zh-CN.md`: synced Chinese row.
  - `internal/domain/spec/README.md`: documented `Diagnostic` export, five fields, and no-leak constraint.
  - `internal/application/audit/README.md`: documented `diagnostics.go` and `report.Result.Diagnostics` field behavior.
- No new decision record required (this DR was updated instead).
- Verification: `make unsupported-diagnostics-evidence-test`, `make parser-error-unsupported-contract-test`, census/feasibility/corpus reports unchanged, `go test ./...` and `go test ./... -tags postgresql` pass, `npm test`, `git diff --check`, `go mod tidy` clean, GitNexus detect_changes confirms expected scope.

## Verification Evidence

- Application-level contract tests: `internal/application/audit/unsupported_diagnostics_evidence_test.go`
- PostgreSQL-tagged contract tests: `internal/application/audit/unsupported_diagnostics_evidence_postgresql_tag_test.go`
- Tests assert: classification, reason, action_hint, audited, dialect, no forbidden payload leak.
- `make ddl-census-report` must confirm unchanged census: MySQL 61/46/0/0/15/0, TiDB 54/45/0/0/9/0, PostgreSQL 285/274/6/0/5/0.
- `make ddl-parser-error-feasibility-report` must confirm unchanged buckets.
- `make parser-error-unsupported-contract-test` must continue passing.
- `make decision-record-gate` must pass.
- Public surface parity tests will be added in Task 3.

### Task 3: Public Surface Diagnostic Evidence Tests

- New test files (4):
  - `pkg/deltascope/audit_unsupported_diagnostics_evidence_test.go` — SDK `Result.Diagnostics` field visibility, parser_error fields, no-leak
  - `internal/interfaces/cli/cli_unsupported_diagnostics_evidence_test.go` — CLI JSON and text output diagnostic visibility
  - `internal/interfaces/http/handler_unsupported_diagnostics_evidence_test.go` — HTTP response diagnostic visibility via `diagnostics` array
  - `internal/interfaces/mcp/server_unsupported_diagnostics_evidence_test.go` — MCP structured content diagnostic visibility
- Additive production changes:
  - `pkg/deltascope/audit.go`: `Result.Diagnostics []spec.Diagnostic` field added; `fromDomainResult` maps diagnostics; `Audit` returns populated result when diagnostics present
  - `internal/interfaces/cli/audit.go`: `hasRenderableAuditResult` checks `Diagnostics`; markdown and quiet renderers output diagnostic fields
  - `internal/interfaces/http/handler.go`: `writeDiagnosticError` includes diagnostics alongside error envelope; `handleAudit` uses it when response has diagnostics
  - `internal/interfaces/http/audit_metadata.go`: `executeOfflineAudit` returns result with diagnostics on error
  - `internal/interfaces/mcp/audit_tool.go`: `auditSQLOffline` and `auditSQLWithMetadata` use `toolDiagnosticError` when diagnostics present
  - `internal/interfaces/mcp/tool_errors.go`: `toolDiagnosticError` function with diagnostics in structured content
- All existing v0.220.0 contract tests: PASS
- All application evidence tests: PASS
- Census unchanged: MySQL 61/46/0/0/15/0, TiDB 54/45/0/0/9/0, PostgreSQL 285/274/6/0/5/0
- No parser support added, no fallback parser, no new rules

### Task 2: Implement Application Diagnostic Evidence

- New type: `spec.Diagnostic` in `internal/domain/spec/statement.go` (5 JSON fields: classification, reason, action_hint, audited, dialect)
- Additive field: `report.Result.Diagnostics []spec.Diagnostic` in `internal/domain/report/result.go`
- New file: `internal/application/audit/diagnostics.go` (constants + helpers)
- Modified: `internal/application/audit/service.go` (2 call sites)
  - Parse-failure path: returns `report.Result{Diagnostics: []spec.Diagnostic{newParserErrorDiagnostic(dialect)}}` with `errParserUnsupported`
  - Unsupported path: appends `newUnsupportedStatementDiagnostic(dialect)` when `result.Unsupported` is non-empty
- PostgreSQL capability-boundary error path unchanged (no parser_error diagnostic for typed errors)
- GitNexus impact on `AuditSQL`: CRITICAL (7 callers) — safe: function signature unchanged, additive result field
- GitNexus impact on `report.Result`: LOW (1 direct caller `Aggregate`) — additive JSON field, no breaking change
- Task 1 RED tests now PASS:
  - `TestParserErrorDiagnosticEvidence`: classification=parser_error, audited=false, reason contains "not audited", action_hint contains "verify the selected dialect", dialect=mysql, no leak
  - `TestUnsupportedStatementDiagnosticEvidencePostgreSQL`: classification=unsupported_statement, audited=false, action_hint contains "review it manually", dialect=postgresql, no leak
- Existing v0.220.0 parser-error unsupported contract tests: all PASS
- Census unchanged: MySQL 61/46/0/0/15/0, TiDB 54/45/0/0/9/0, PostgreSQL 285/274/6/0/5/0

## Consequences

- Future diagnostic additions must preserve the no-leak contract.
- Public surface tests (Task 3) will lock in the evidence contract across all transports.
- If a future milestone adds fallback parsing, parser_error diagnostics should be reclassified rather than removed.
- Census and feasibility counts must remain unchanged through this milestone.

## Links

- Commits: 0bdf52b (base)
- Tests: `internal/application/audit/unsupported_diagnostics_evidence_test.go`, `internal/application/audit/unsupported_diagnostics_evidence_postgresql_tag_test.go`
- Docs: `docs/plans/2026-05-28-v0.230.0-unsupported-diagnostics-evidence-pack/design.md`
- Related: `docs/decisions/2026-05-28-v0.220.0-parser-error-unsupported-contract-hardening.md`

# 审计能力补完任务提示词

> 用于 `Audit Completion` 里程碑的逐任务实施与评审。
> 每个提示词都假设工作发生在 `/Users/fan/GolangProjects/deltascope` 中。

## 全局规则

- 保持现有离线优先引擎不变。
- 将元数据作为可选事实接入，而不是引入第二条审计管线。
- 让规则继续消费规范化 spec 与快照，而不是原始 DB 客户端。
- 对每个非平凡规则或编排变更使用 TDD。
- 将 `three-level-doc` 视为硬门槛。
- 每个任务都返回变更文件、运行测试、状态和 commit hash。

## 里程碑重点

- 以能力矩阵作为验收事实来源
- 实例事实与对象快照
- 元数据感知的可选增强
- 更深入的 alter 兼容性
- 剩余重要 DDL/DML 缺口补齐
- 更成熟的产品文档与发布面

## 任务意图

### 任务 1：能力矩阵

- 产出一份具体的重要审计能力矩阵。
- 将每一行标记为已覆盖、已替代、已延期或不在范围内。
- 精确识别哪些行仍阻塞里程碑完成。

### 任务 2：元数据领域抽象

- 为实例事实与表快照增加规范化领域类型。
- 不要将 SQL 客户端或 parser 专属对象泄漏到领域模型中。

### 任务 3：元数据提供者

- 增加可选 provider 接口与基础设施实现。
- 当未配置 metadata provider 时，必须保留完全相同的离线路径。

### 任务 4：存在性与快照规则

- 增加依赖对象存在性与当前表形状的规则。
- 为每条已发布规则接入默认配置与配置模板条目。

### 任务 5：Alter 兼容性

- 深化源到目标的兼容性检查。
- 在元数据或源事实仍不足时，保持诚实。

### 任务 6：剩余缺口补齐

- 用矩阵，而不是直觉，来选择剩余规则工作。
- 优先补高价值缺口，而不是追求低价值广度。

### 任务 7：产品文档

- 使用 shields 与快速链接重写 README。
- 增加 `README_ZH.md`、`CHANGELOG.md` 与 `SECURITY.md`。
- 保持中英文 README 的结构对齐。

### 任务 8：收口

- 重新运行完整验证。
- 更新 handoff、autonomous-progress 与 decisions。
- 完成能力矩阵并推送里程碑。
name: salary_filter_projection
dialect: postgresql
mode: projection_only
expect:
  read_classification: indeterminate
  admission: indeterminate
  relations:
    - name: users
      kind: table
      permission_required: true
  referenced_columns:
    - table: users
      column: id
      usages: [projection]
    - table: users
      column: salary
      usages: [filter]
  outputs:
    - name: id
      sources: [users.id]
  requirements:
    - object: users
      privilege: read_table
    - object: users.id
      privilege: read_column
  unresolved: []
  warnings: [projection_only_inference_risk]
  reason_codes: []

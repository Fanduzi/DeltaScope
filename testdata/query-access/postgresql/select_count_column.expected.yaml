name: select_count_column
dialect: postgresql
mode: strict
expect:
  # T2 count(anyelement) candidate OID 2147; unproven → indeterminate.
  read_classification: indeterminate
  admission: indeterminate
  relations:
    - name: users
      kind: table
      permission_required: true
  referenced_columns:
    - table: users
      column: id
      usages: [projection]
  outputs: []
  requirements:
    - object: users
      privilege: read_table
    - object: users.id
      privilege: read_column
  unresolved: []
  warnings: []
  reason_codes: [unproven_function_effect]

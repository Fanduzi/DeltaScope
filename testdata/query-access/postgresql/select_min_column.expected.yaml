name: select_min_column
dialect: postgresql
mode: strict
expect:
  read_classification: indeterminate
  admission: indeterminate
  relations:
    - name: orders
      kind: table
      permission_required: true
  referenced_columns:
    - table: orders
      column: amount
      usages: [projection]
  outputs: []
  requirements:
    - object: unqualified_relation
      privilege: indeterminate
  unresolved:
    - reference: unqualified_relation
      reason: unqualified_relation_blocked
  warnings: []
  reason_codes: [unproven_function_effect, unqualified_relation_blocked]

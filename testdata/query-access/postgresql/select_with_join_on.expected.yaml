name: select_with_join_on
dialect: postgresql
mode: strict
expect:
  read_classification: indeterminate
  admission: indeterminate
  relations:
    - name: orders
      alias: o
      kind: table
      permission_required: true
    - name: users
      alias: u
      kind: table
      permission_required: true
  referenced_columns:
    - table: orders
      column: user_id
      usages: [join]
    - table: users
      column: id
      usages: [join]
  outputs:
    - name: "*"
      sources: ["*"]
  requirements:
    - object: unqualified_relation
      privilege: indeterminate
  unresolved:
    - reference: unqualified_relation
      reason: unqualified_relation_blocked
  warnings: []
  reason_codes: [unproven_operator_effect, unqualified_relation_blocked]

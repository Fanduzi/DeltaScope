name: existing_table_update
dialect: mysql
category: dml
expect:
  parse_ok: true
  statement_kind: dml
  operation: update
  unsupported:
    count: 0
  findings:
    exclude:
      - dml.table.exists.require
metadata:
  schema: app
  tables:
    existing_users:
      exists: true

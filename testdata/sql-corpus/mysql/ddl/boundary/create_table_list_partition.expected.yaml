name: create_table_list_partition
dialect: mysql
category: ddl
expect:
  parse_ok: true
  statement_kind: ddl
  operation: create_table
  statements:
    count: 1
  unsupported:
    count: 0
  findings:
    include:
      - ddl.column.default.require
      - ddl.table.partition.forbid
      - ddl.table.primary_key.columns.max_count
    exclude:
      - ddl.table.primary_key.require

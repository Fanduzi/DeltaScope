name: create_table_primary_key
dialect: tidb
category: ddl
expect:
  parse_ok: true
  statement_kind: ddl
  unsupported:
    count: 0
  findings:
    include: []
    exclude:
      - ddl.table.primary_key.require
      - ddl.table.primary_key.not_null.require

name: create_table_exists_forbid
dialect: postgresql
category: ddl
expect:
  parse_ok: true
  statement_kind: ddl
  operation: create_table
  unsupported:
    count: 0
  findings:
    include:
      - ddl.table.exists.create.forbid
      - ddl.table.denylist.forbid
config:
  rules:
    ddl.table.denylist.forbid:
      enabled: true
      params:
        tables: [users]
metadata:
  schema: public
  tables:
    users:
      exists: true
